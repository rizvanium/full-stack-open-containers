FROM node:18.14.2

WORKDIR /usr/src/app

ARG BACKEND_URI
ARG WS_BACKEND_URI

ENV REACT_APP_BACKEND_URI=$BACKEND_URI
ENV REACT_APP_WS_BACKEND_URI=$WS_BACKEND_URI

COPY . .

RUN npm install

CMD ["npm", "start"]